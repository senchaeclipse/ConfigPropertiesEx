Ext.define("MyApp.CfgEx2", {

	extend : 'Ext.Panel',
	/**
	 * @cfg {String} [foo=""] The ddgroup name for the MultiSelect DropZone.
	 */
	/**
	 * @cfg {Date} [bar=""] The ddgroup name for the MultiSelect DropZone.
	 */
	constructor : function(config) {
		 
	}

})
